import { AsyncReadable } from '../lib/async-readable';





describe('AsyncReadable', () => {

    test('import', () => {
        expect(AsyncReadable).toBeDefined();
    });


    describe('TODO', () => {
        xtest('', () => { });
    });

});

